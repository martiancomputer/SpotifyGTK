# Nightly track -- native PipeWire audio output.
#
# Usage:
#   meson setup build --native-file build-profiles/nightly.ini --buildtype=release

[built-in options]
buildtype = 'release'

[project options]
profile         = 'nightly'
enable_pipewire = true
