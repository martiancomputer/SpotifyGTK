# Stable track -- PulseAudio/ALSA only, broadest compatibility.
#
# Usage:
#   meson setup build --native-file build-profiles/stable.ini --buildtype=release

[built-in options]
buildtype = 'release'

[project options]
profile         = 'stable'
enable_pipewire = false
