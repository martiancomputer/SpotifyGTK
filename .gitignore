# Meson build dirs
build/
_build/
builddir/
# librespot reference repository (external, not part of project)
librespot-repo/
 
# Compiled output
*.o
*.so
*.a
spotifygtk
# Build directories
build/
*-build/
 
# Editor
# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~
.clangd/
compile_commands.json
 
# Config / tokens (NEVER commit credentials)
*.env
.env
config.local.h
 
# OS
.DS_Store

# perf profiling output (local only)
perf.data
perf.data.old

# The Windows VM is an environment, not part of the project: scripts, answer
# file, disk images and the rebuilt ISO all stay local.
apps/spotify-native-windows/vm/
apps/spotify-native-windows/build/

# Session logs written beside the executable by log_file.c
spotify-native.log
spotify-native.prev.log
.local-backups/

# Profiling build and heaptrack captures
build-prof/
heaptrack.*.zst
