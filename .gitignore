# Meson build dirs
build/
_build/
builddir/
# librespot reference repository (external, not part of project)
librespot-repo/
 
# Compiled output
*.o
*.so
*.a
spotifygtk
# Build directories
build/
*-build/
 
# Editor
# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~
.clangd/
compile_commands.json
 
# Config / tokens (NEVER commit credentials)
*.env
.env
config.local.h
 
# OS
.DS_Store

# perf profiling output (local only)
perf.data
perf.data.old

# Windows build VM artefacts (disk images, firmware vars, guest share)
apps/spotify-native-windows/vm/*.qcow2
apps/spotify-native-windows/vm/OVMF_VARS.fd
apps/spotify-native-windows/vm/share/
apps/spotify-native-windows/build/
